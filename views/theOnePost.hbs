
<main class="flex-shrink-0">
  <div class="container">
    <h1 class="mb-5 mt-5">{{thePost.profession}}</h1>
    <iframe style="width: 100%; height: 500px" class="mb-3" src={{thePost.videoLink}} allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
    <p class="lead"> <h3>{{thePost.title}}</h3> </p>
    <p class="lead">{{thePost.script}}</p>
    <a class="nav-linkPost text-decoration-none text-success" href="/video/{{thePost.case.caseTitle}}" tabindex="-1" aria-disabled="true"> <h4>Решить реальный кейс</h4></a>
    <form action="/vote/{{thePost._id}}" method="GET" class="btn-group mt-2">
      <button class="btn btn-sm btn-outline-secondary">Рейтинг: {{thePost.rating}} (проголосовать)</button>
    </form>
    <form action="/fav/{{thePost._id}}" method="GET" class="btn-group mt-2">
      <button class="btn btn-sm btn-outline-secondary">В любимом у {{thePost.favourites}} (добавить)</button>
    </form>
  </div>
</main>
